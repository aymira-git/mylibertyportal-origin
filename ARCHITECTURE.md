# MYLIBERTY Architecture

The active architecture guide lives at [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md).

Before structural changes, read that guide and preserve the project's protected infrastructure, domain boundaries (`src/features/*`), and incremental migration rules.

For an overview of all project documentation, specifications, and roadmaps, see [`docs/README.md`](./docs/README.md).
