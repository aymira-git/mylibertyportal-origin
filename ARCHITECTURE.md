# MYLIBERTY Architecture

The active architecture guide lives at `src/ARCHITECTURE.md`.

Before structural changes, read that guide and preserve the project's protected infrastructure and incremental migration rules.
